# shell

1. `basic_mediainfo.sh`
   用 `mediainfo` 显示多媒体文件信息并用 `jq` 提取主要信息成一行文本
2. `install_java_bin.sh`  
   安装 java Binary 包
3. `lasync.sh`  
   使用 rsync 进行同步
4. `macos_brew_install.sh`  
   macOS 上用 homebrew 安装常用软件
5. `profile.sh`  
   Shell 登录脚本
